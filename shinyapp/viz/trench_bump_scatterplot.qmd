---
title: "kasdhflaksjdhflaskjfh"
format: html
editor: visual
---

## Quarto

Quarto enables you to weave together content and executable code into a finished document. To learn more about Quarto see <https://quarto.org>.

## Running Code

When you click the **Render** button a document will be generated that includes both content and the output of embedded code. You can embed code like this:

```{r}
library(tidyverse)
library(dplyr)
library(readr)
library(ggplot2)
library(ggbeeswarm)

read_data <- read.csv("../data/test_data/data.csv")

team_list<- c("449","2988")

filtered_df <- read_data |> filter(team %in% team_list)

combined_more <- filtered_df |>
    group_by(team) |>
    summarize(avg_trench = mean(teleop_trench), 
              avg_bump = mean(teleop_bump))

ggplot(combined_more, aes(x = avg_trench, y = avg_bump)) + 
    geom_point() +
    geom_label(
    label = combined_more$team,
    nudge_x = 0.25, nudge_y = 0.25,
    check_overlap = T
  ) +
    scale_x_continuous(expand = c(0, 0), limits = c(0, max(combined_more$avg_trench + 1))) + 
    scale_y_continuous(expand = c(0, 0), limits = c(0, max(combined_more$avg_bump + 1))) +
    labs(title = "Mean Crossing Comparison",
         x = "Trench count",
         y = "Bump count") + 
    theme_minimal()
```

You can add options to executable code like this

```{r}
#| echo: false
2 * 2
```

The `echo: false` option disables the printing of code (only output is displayed).
